Change theme via /usr/lib/sddm/sddm.conf.d/default.conf

Add "custom" folder to /usr/share/sddm/themes

## Useful for testing the theme

## `sddm-greeter --test-mode --theme /usr/share/sddm/themes/custom`
